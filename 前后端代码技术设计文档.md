## 电力设备智能运维系统 — 前后端代码技术设计文档

本文档基于项目实际代码，完整描述「基于大语言模型的电力设备智能运维分析与决策支持系统」的前后端架构设计、模块划分、数据模型、接口定义与页面实现。

---

### 一、系统总体架构

系统采用前后端分离架构，后端使用 Python FastAPI 提供 RESTful API 与 WebSocket 实时通信服务，前端使用 Vue 3 + TypeScript 构建单页应用（SPA）。当前版本为原型验证阶段，数据层全部使用 Mock 数据，API 服务层已预置对接接口，待接入真实模型与数据库后可无缝切换。

```
┌───────────────────────────────────────────────────────────┐
│                      浏览器 (SPA)                          │
│  Vue 3 + TypeScript + Element Plus + ECharts + Pinia      │
└────────────────────────┬──────────────────────────────────┘
                         │ HTTP / WebSocket
                         ▼
┌───────────────────────────────────────────────────────────┐
│                   FastAPI (Python)                         │
│  Router → Service → Mock Data                             │
│  6 组 RESTful API + 1 组 WebSocket                        │
└───────────────────────────────────────────────────────────┘
```

**项目目录结构：**

```
power-equipment-intelligent-om/
├── .gitignore
├── backend/                     # Python 后端
│   ├── run.py                   # Uvicorn 启动脚本
│   ├── requirements.txt         # Python 依赖
│   └── app/
│       ├── main.py              # FastAPI 应用工厂、中间件、异常处理、路由注册
│       ├── config.py            # Pydantic Settings 配置
│       ├── models/              # Pydantic 数据模型 (7 个模块)
│       ├── routers/             # API 路由层 (6 个模块)
│       ├── services/            # 业务逻辑层 (6 个模块)
│       └── mock_data/           # Mock 数据层 (5 个模块)
└── frontend/                    # Vue 前端
    ├── package.json
    ├── vite.config.ts
    └── src/
        ├── main.ts              # 应用入口
        ├── App.vue              # 根组件
        ├── router/index.ts      # 路由配置
        ├── views/               # 4 个业务页面
        ├── components/          # 5 个通用/布局组件
        ├── stores/              # 4 个 Pinia Store
        ├── services/api.ts      # Axios API 客户端
        ├── types/index.ts       # TypeScript 类型定义
        └── assets/styles/       # SCSS 主题样式
```

---

### 二、后端架构设计

#### 2.1 技术栈与依赖

| 依赖 | 版本 | 用途 |
|---|---|---|
| fastapi | >=0.110.0 | Web 框架 |
| uvicorn[standard] | >=0.27.0 | ASGI 服务器 |
| pydantic | >=2.6.0 | 数据校验与序列化 |
| pydantic-settings | >=2.1.0 | 配置管理（环境变量注入） |
| python-multipart | >=0.0.9 | 文件上传支持（预留） |
| websockets | >=12.0 | WebSocket 实时通信 |

#### 2.2 分层架构

后端严格遵循三层架构，每层职责清晰：

```
Router (路由层) → Service (服务层) → Mock Data (数据层)
```

**路由层**负责 HTTP 请求解析、参数校验、调用 Service 并返回统一 `ApiResponse[T]` 响应。**服务层**封装业务逻辑，所有方法声明为 `async`（为后续接入真实数据库/LLM 做预留）。**Mock 数据层**提供静态数据与模拟计算函数。

依赖注入通过 FastAPI 的 `Depends()` 实现，每个 Router 使用工厂函数返回模块级单例 Service 实例。

#### 2.3 应用配置

`app/main.py` 中 FastAPI 应用的核心配置：

- 应用名称：「电力设备智能运维分析与决策支持系统」
- API 文档：`/docs`（Swagger UI）、`/redoc`（ReDoc）
- CORS 中间件：允许 `localhost:5173`、`localhost:3000` 等前端开发地址
- 全局异常处理器（3个）：所有异常均返回 HTTP 200，错误信息放在响应体 `code` 字段中（符合国内企业 API 惯例）

| 异常处理器 | 触发条件 | 返回 code |
|---|---|---|
| `biz_exception_handler` | 自定义 `BizException` | 自定义（默认 400） |
| `validation_exception_handler` | Pydantic `RequestValidationError` | 422 |
| `global_exception_handler` | 未捕获 `Exception` | 500 |

**配置管理**（`config.py`）基于 `pydantic-settings`，所有配置项可通过 `POWER_OM_` 前缀的环境变量覆盖：

| 配置项 | 类型 | 默认值 |
|---|---|---|
| `app_name` | str | "电力设备智能运维分析与决策支持系统" |
| `app_version` | str | "1.0.0" |
| `debug` | bool | True |
| `api_prefix` | str | "/api/v1" |
| `cors_origins` | list[str] | 4个本地开发地址 |
| `ws_heartbeat_interval` | int | 30（秒） |

#### 2.4 API 接口总览

共 14 个端点（13 个 HTTP + 1 个 WebSocket）：

| # | 方法 | 路径 | 说明 | 请求模型 | 响应模型 |
|---|---|---|---|---|---|
| 1 | GET | `/` | 欢迎页 | — | 版本与文档链接 |
| 2 | GET | `/health` | 健康检查 | — | `{status: "healthy"}` |
| 3 | POST | `/api/v1/chat` | 运维智能问答 | `ChatRequest` | `ChatResponse` |
| 4 | POST | `/api/v1/diagnosis/trigger` | 触发故障诊断 | `DiagnosisTriggerRequest` | `DiagnosisTriggerResponse` |
| 5 | GET | `/api/v1/diagnosis/{id}` | 获取诊断报告 | path: `diagnosis_id` | `DiagnosisReport` |
| 6 | WS | `/ws/diagnosis/{id}` | 诊断进度实时推送 | path: `diagnosis_id` | JSON 事件流 |
| 7 | POST | `/api/v1/batch/assess` | 批次风险评估 | `BatchAssessRequest` | `BatchAssessResponse` |
| 8 | POST | `/api/v1/maintenance/plan` | 生成检修计划 | `MaintenancePlanRequest` | `MaintenancePlanResponse` |
| 9 | POST | `/api/v1/knowledge/upload` | 上传知识文档 | `KnowledgeUploadRequest` | `dict` |
| 10 | GET | `/api/v1/knowledge/search` | 知识语义检索 | query: `query, top_k, domain` | `KnowledgeSearchResponse` |
| 11 | GET | `/api/v1/equipment` | 设备列表（分页） | query: 6个筛选参数 | `EquipmentListResponse` |
| 12 | GET | `/api/v1/equipment/stats` | 设备统计概览 | — | `EquipmentStatsResponse` |
| 13 | GET | `/api/v1/equipment/{id}/health` | 设备健康度评分 | path: `equipment_id` | `HealthResponse` |
| 14 | GET | `/api/v1/equipment/{id}` | 设备详情 | path: `equipment_id` | `EquipmentDetail` |

所有端点返回统一格式 `ApiResponse[T]`：

```json
{
  "code": 0,
  "message": "success",
  "data": { ... }
}
```

#### 2.5 数据模型定义（Pydantic Models）

##### 2.5.1 通用模型（`models/common.py`）

```python
class ApiResponse(GenericModel, Generic[T]):
    code: int = 0
    message: str = "success"
    data: Optional[T] = None

class PaginationParams(BaseModel):
    page: int = 1
    page_size: int = 10
```

##### 2.5.2 智能问答模型（`models/chat.py`）

```python
class ChatRequest(BaseModel):
    message: str           # 用户消息，1-2000字符
    conversation_id: str = "default"
    equipment_id: Optional[str] = None

class Reference(BaseModel):
    title: str
    source: str
    relevance: float       # 0-1

class ChatResponse(BaseModel):
    answer: str            # AI 回答
    references: list[Reference] = []
    confidence: float      # 置信度 0-1
    conversation_id: str
```

##### 2.5.3 故障诊断模型（`models/diagnosis.py`）

```python
class SensorDataInput(BaseModel):
    sensor_type: str
    value: float
    unit: str = ""
    threshold: Optional[float] = None

class DiagnosisTriggerRequest(BaseModel):
    equipment_id: str
    event_type: str        # "dga_alarm" | "temperature_alarm" | "pd_alarm"
    sensor_data: list[SensorDataInput] = []

class DiagnosisTriggerResponse(BaseModel):
    diagnosis_id: str      # 格式: "DIAG-XXXXXXXX"
    status: str = "processing"

class EvidenceItem(BaseModel):
    step: int
    evidence: str
    confidence: float      # 0-1

class RootCauseAnalysis(BaseModel):
    conclusion: str
    evidence_chain: list[EvidenceItem]
    confidence: float

class BatchRisk(BaseModel):
    batch_id: str
    risk_level: str
    affected_devices: list[str]
    recommendation: str

class CorrectiveAction(BaseModel):
    priority: int
    action: str
    timeframe: str

class EquipmentInfo(BaseModel):
    equipment_id: str
    name: str
    type: str
    model: str
    manufacturer: str
    voltage_level: str
    substation: str

class DiagnosisReport(BaseModel):
    diagnosis_id: str
    status: str = "completed"
    equipment: Optional[EquipmentInfo]
    fault_symptom: str
    data_analysis: dict
    root_cause: Optional[RootCauseAnalysis]
    batch_risk: Optional[BatchRisk]
    corrective_actions: list[CorrectiveAction]
    reasoning_trace: list[dict]
    created_at: str
    completed_at: str
```

##### 2.5.4 批次评估模型（`models/batch.py`）

```python
class BatchAssessRequest(BaseModel):
    equipment_id: str
    batch_criteria: dict = {"manufacturer": True, "model": True, "year": True}

class RiskDevice(BaseModel):
    equipment_id: str
    name: str
    health_index: float
    risk_level: str        # "高" | "中" | "低"
    risk_score: float
    issue_description: str

class BatchReport(BaseModel):
    batch_description: str
    total_count: int
    risk_summary: dict
    statistical_analysis: dict
    recommendations: list[str]
    risk_level: str = "低"

class BatchAssessResponse(BaseModel):
    batch_size: int
    risk_devices: list[RiskDevice]
    batch_report: BatchReport
```

##### 2.5.5 检修决策模型（`models/maintenance.py`）

```python
class MaintenancePlanRequest(BaseModel):
    equipment_ids: list[str]   # 至少1个
    time_horizon: int = 90     # 7-365天

class WorkOrder(BaseModel):
    order_id: str
    equipment_id: str
    equipment_name: str
    task_type: str
    priority: int              # 1-5
    planned_date: str
    estimated_hours: int
    description: str = ""
    status: str = "planned"

class MaintenancePlanItem(BaseModel):
    equipment_id: str
    equipment_name: str
    health_index: float
    plan_type: str             # "例行维护" | "状态检修" | "专项检修" | "紧急检修"
    recommended_actions: list[str]
    planned_date: str
    urgency: str = "normal"    # "low" | "normal" | "high" | "critical"

class MaintenancePlanResponse(BaseModel):
    plans: list[MaintenancePlanItem]
    work_orders: list[WorkOrder]

class HealthIndicator(BaseModel):
    name: str
    score: float
    weight: float
    status: str
    details: str

class TrendPoint(BaseModel):
    date: str
    health_index: float

class HealthResponse(BaseModel):
    equipment_id: str
    health_index: float        # 0-100
    assessment_date: str
    indicators: dict
    trend: list[TrendPoint]
    assessment_result: str
    recommendation: str
```

##### 2.5.6 知识管理模型（`models/knowledge.py`）

```python
class KnowledgeUploadRequest(BaseModel):
    title: str
    content: str
    domain: str = "通用"
    doc_type: str = "运维知识"
    source: str = "用户上传"
    keywords: list[str] = []

class KnowledgeSearchRequest(BaseModel):
    query: str
    top_k: int = 5             # 1-20
    domain: Optional[str] = None

class KnowledgeItem(BaseModel):
    doc_id: str
    title: str
    domain: str
    type: str
    source: str
    content: str
    relevance: float

class KnowledgeSearchResponse(BaseModel):
    query: str
    total: int
    results: list[KnowledgeItem]
```

##### 2.5.7 设备管理模型（`models/equipment.py`）

```python
class EquipmentDetail(BaseModel):
    equipment_id: str
    name: str
    type: str                  # "transformer" | "breaker" | "gis"
    type_name: str
    model: str
    manufacturer: str
    voltage_level: str
    capacity: str
    substation: str
    substation_id: str
    commission_date: str
    status: str                # "normal" | "warning" | "fault" | "maintenance"
    status_name: str
    health_index: float
    location: str = ""
    longitude: float = 0
    latitude: float = 0
    last_maintenance_date: str = ""
    next_maintenance_date: str = ""
    operating_hours: int = 0

class EquipmentListResponse(BaseModel):
    total: int
    page: int
    page_size: int
    items: list[EquipmentDetail]

class EquipmentStatsResponse(BaseModel):
    total: int
    by_type: dict
    by_status: dict
    by_substation: dict
    avg_health_index: float
    warning_count: int
    fault_count: int
```

#### 2.6 业务服务层设计

##### 2.6.1 ChatService — 智能问答

核心方法 `chat(request)` 的处理流程：

1. 调用 `knowledge.search_knowledge(message, top_k=5)` 进行知识检索
2. 基于关键词匹配从 5 个领域模板中选择回答（温度、DGA、局放、断路器、维护）
3. 若指定 `equipment_id`，追加设备上下文信息
4. 计算置信度：`0.4 × max_relevance + 0.4 × avg_relevance + 0.2 × coverage`
5. 记录会话历史到内存 `_conversations` 字典

##### 2.6.2 DiagnosisService — 故障诊断

核心方法 `trigger_diagnosis(request)` 的处理流程：

1. 生成诊断 ID（格式 `DIAG-XXXXXXXX`）
2. 根据设备类型获取传感器数据（DGA、局放等）
3. 根据 `event_type`（dga_alarm / temperature_alarm / pd_alarm）生成根因分析、故障症状、批次风险、修复建议
4. 构建 6 步推理链路（`reasoning_trace`）模拟多 Agent 协作过程

WebSocket 端点 `/ws/diagnosis/{id}` 推送 14 个事件，模拟 5 个 Agent 的协作流水线：
- Data Collection Agent → Feature Analysis Agent → Knowledge Reasoning Agent → Batch Assessment Agent → Decision Advisor Agent
- 每个 Agent 依次推送 `agent_start`、`agent_progress`、`agent_complete` 事件
- 最终推送 `diagnosis_done` 和 `review_result` 事件

##### 2.6.3 BatchService — 批次风险评估

核心方法 `assess(request)` 的处理流程：

1. 根据 `batch_criteria` 筛选同批次设备（厂家匹配、型号前缀匹配、投运年份5年内）
2. 对健康指数低于 85 的设备生成风险记录（<60 为高风险，否则为中风险）
3. 计算统计分析：均值/标准差/最小值/最大值/低于阈值数量
4. 根据高风险设备数量判定批次风险等级（高/中/低）

##### 2.6.4 MaintenanceService — 检修决策

核心方法 `generate_plan(request)` 的处理流程：

1. 对每个设备获取健康度数据
2. 根据健康指数映射检修类型和紧急程度：

| 健康指数 | 检修类型 | 紧急程度 |
|---|---|---|
| >= 90 | 例行维护 | low |
| >= 70 | 状态检修 | normal |
| >= 50 | 专项检修 | high |
| < 50 | 紧急检修 | critical |

3. 根据紧急程度和 `time_horizon` 计算计划日期
4. 按设备类型生成推荐操作（变压器/断路器/GIS 各有专属操作列表）
5. 生成工单（WorkOrder），按优先级排序

##### 2.6.5 KnowledgeService — 知识管理

- `upload(request)`：生成文档 ID（`KB-UPL-XXXXXX`），追加到知识库
- `search(request)`：委托 `mock_data.knowledge.search_knowledge()` 执行关键词匹配检索

##### 2.6.6 EquipmentService — 设备管理

- `list_equipment(...)`：支持按类型、状态、变电站、关键词筛选，分页返回
- `get_equipment(id)`：按 ID 查询单台设备
- `get_stats()`：聚合统计（按类型/状态/变电站分组，平均健康指数，告警/故障计数）

#### 2.7 Mock 数据层设计

##### 2.7.1 设备数据（`mock_data/equipment.py`）

预置 **12 台设备**，分布在 4 个变电站：

| ID | 名称 | 类型 | 厂家 | 电压等级 | 变电站 | 健康指数 | 状态 |
|---|---|---|---|---|---|---|---|
| TR-001 | 1号主变 | 变压器 | 特变电工 | 220kV | 滨江变电站 | 92.5 | 正常 |
| TR-002 | 2号主变 | 变压器 | 特变电工 | 220kV | 滨江变电站 | 78.3 | 告警 |
| TR-003 | 1号主变 | 变压器 | 西电集团 | 220kV | 西湖变电站 | 95.2 | 正常 |
| TR-004 | 3号主变 | 变压器 | 保定天威 | 110kV | 余杭变电站 | 68.7 | 告警 |
| TR-005 | 2号主变 | 变压器 | 西电集团 | 500kV | 萧山变电站 | 97.8 | 正常 |
| CB-001 | 220kV滨西线断路器 | 断路器 | 平高电气 | 220kV | 滨江变电站 | 88.9 | 正常 |
| CB-002 | 220kV滨东线断路器 | 断路器 | 平高电气 | 220kV | 滨江变电站 | 91.2 | 正常 |
| CB-003 | 110kV余北线断路器 | 断路器 | 河南森源 | 110kV | 余杭变电站 | 45.2 | 故障 |
| CB-004 | 500kV萧浦线断路器 | 断路器 | ABB | 500kV | 萧山变电站 | 94.6 | 正常 |
| GIS-001 | 220kV GIS | GIS | 西开电气 | 220kV | 西湖变电站 | 96.1 | 正常 |
| GIS-002 | 110kV GIS | GIS | 正泰电气 | 110kV | 余杭变电站 | 89.5 | 正常 |
| GIS-003 | 500kV GIS | GIS | ABB | 500kV | 萧山变电站 | 85.3 | 检修中 |

每台设备还包含类型专属扩展字段：变压器有额定电流、阻抗电压、油重等；断路器有开断容量、SF6气压、操作次数等；GIS有间隔数、气室数等。

##### 2.7.2 传感器数据（`mock_data/sensors.py`）

提供三类传感器数据生成器：

**时序数据**（`get_sensor_data`）：支持油温、绕组温度、负荷电流、振动 4 种传感器。使用正弦日周期 + 线性趋势 + 随机噪声生成公式。TR-002、TR-004 具有明显上升趋势，模拟劣化设备。

**DGA 数据**（`get_dga_data`）：生成 12 个月的 7 种溶解气体（H2、CH4、C2H2、C2H4、C2H6、CO、CO2）月度记录，附带 IEC 三比值法自动诊断（正常老化 / 电弧放电 / 低温过热 / 高温过热 / 局部放电）。

**局放数据**（`get_partial_discharge_data`）：生成 7 天逐小时局放时序数据（含 5% 概率脉冲尖峰）和 36 相位区间 PRPD 谱图数据。

##### 2.7.3 故障案例（`mock_data/faults.py`）

预置 **6 个故障案例**：

| 案例 | 设备 | 故障类型 | 严重等级 |
|---|---|---|---|
| FC-001 | TR-002 | 绕组高温过热 | 严重 |
| FC-002 | CB-003 | SF6气体压力下降 | 一般 |
| FC-003 | GIS-002 | 自由颗粒放电 | 严重 |
| FC-004 | TR-004 | 套管介损超标 | 一般 |
| FC-005 | CB-001 | 操动机构卡涩 | 严重 |
| FC-006 | TR-005 | 铁芯多点接地 | 一般 |

每个案例包含：故障症状列表、根因分析、证据链（含置信度）、修复措施、预防方案、修复耗时和费用。

##### 2.7.4 知识库（`mock_data/knowledge.py`）

预置 **13 份知识文档**，覆盖变压器（6份）、断路器（3份）、GIS（2份）、通用（2份）四个领域，涵盖标准规程、诊断知识、维护策略、应急预案、风险评估五种类型。

检索函数 `search_knowledge(query, top_k, domain)` 使用关键词匹配模拟语义检索：关键词命中 +0.3，领域匹配 +0.2，标题词匹配 +0.15，正文词匹配 +0.1，最终乘以文档自身 `relevance_score` 权重。

##### 2.7.5 健康度数据（`mock_data/health.py`）

为全部 12 台设备提供健康度画像，每台设备包含 4 个维度指标（按设备类型区分）：

| 设备类型 | 维度1 | 维度2 | 维度3 | 维度4 |
|---|---|---|---|---|
| 变压器 | 绝缘状态(40%) | 热状态(25%) | 电气状态(25%) | 机械状态(10%) |
| 断路器 | 机械特性(35%) | 绝缘状态(30%) | 电气状态(25%) | 密封状态(10%) |
| GIS | 绝缘状态(40%) | 机械特性(25%) | 密封状态(25%) | 监测状态(10%) |

同时生成 13 个月的健康趋势数据点。

#### 2.8 模块依赖关系

```
run.py
  └── app.main:app
        ├── app.config.get_settings()
        ├── app.routers.chat        → ChatService
        ├── app.routers.diagnosis   → DiagnosisService
        ├── app.routers.batch       → BatchService
        ├── app.routers.maintenance → MaintenanceService
        ├── app.routers.knowledge   → KnowledgeService
        └── app.routers.equipment   → EquipmentService + MaintenanceService

Service → Mock Data 依赖:
  ChatService        → knowledge.search_knowledge, equipment.get_equipment_by_id
  DiagnosisService   → equipment, sensors, faults, knowledge
  BatchService       → equipment, faults
  MaintenanceService → equipment, health
  KnowledgeService   → knowledge
  EquipmentService   → equipment
```

---

### 三、前端架构设计

#### 3.1 技术栈与依赖

| 分类 | 依赖 | 版本 | 用途 |
|---|---|---|---|
| 核心 | vue | ^3.4.21 | 前端框架 |
| | vue-router | ^4.3.0 | 路由管理 |
| | pinia | ^2.1.7 | 状态管理 |
| UI | element-plus | ^2.6.1 | UI 组件库 |
| | @element-plus/icons-vue | ^2.3.1 | 图标库 |
| 可视化 | echarts | ^5.5.0 | 图表库 |
| 网络 | axios | ^1.6.7 | HTTP 客户端 |
| 工具 | marked | ^12.0.0 | Markdown 解析（已声明但未使用） |
| 构建 | vite | ^5.1.6 | 构建工具 |
| | typescript | ~5.4.0 | 类型系统 |
| | sass | ^1.71.1 | SCSS 预处理器 |
| 自动导入 | unplugin-auto-import | ^0.17.5 | Vue/Router/Pinia API 自动导入 |
| | unplugin-vue-components | ^0.26.0 | Element Plus 组件自动解析 |

#### 3.2 构建配置

**Vite 配置**（`vite.config.ts`）：

- 路径别名：`@` → `./src`
- 自动导入：`vue`、`vue-router`、`pinia` 全局 API，Element Plus 组件自动解析（SASS 按需导入）
- SCSS 全局注入：`variables.scss` 通过 `additionalData` 注入到每个 SCSS 块，所有变量无需显式 import 即可使用
- 开发代理：`/api` 请求代理到 `http://localhost:8000`（后端）

**TypeScript 配置**：

- 目标：ES2020，模块：ESNext，严格模式开启
- 路径别名：`@/*` → `./src/*`
- 宽松选项：`noUnusedLocals` 和 `noUnusedParameters` 关闭

**入口引导**（`main.ts`）：

1. 创建 Vue 应用
2. 注册全部 Element Plus 图标为全局组件
3. 安装 Pinia（状态管理）
4. 安装 Vue Router（路由）
5. 安装 Element Plus（默认尺寸）
6. 导入全局 SCSS 样式
7. 挂载到 `#app`

#### 3.3 路由设计

采用 HTML5 History 模式，所有页面嵌套在 `AppLayout` 布局组件中，4 个业务页面均使用懒加载实现代码分割：

| 路径 | 名称 | 组件 | Meta（标题/图标） | 懒加载 |
|---|---|---|---|---|
| `/` | (redirect) | `AppLayout` | — | 否 |
| `/dashboard` | Dashboard | `DashboardView.vue` | 可视化大屏 / DataAnalysis | 是 |
| `/chat` | Chat | `ChatView.vue` | 运维问答 / ChatDotRound | 是 |
| `/diagnosis` | Diagnosis | `DiagnosisView.vue` | 故障诊断 / FirstAidKit | 是 |
| `/maintenance` | Maintenance | `MaintenanceView.vue` | 维护决策 / SetUp | 是 |

默认 `/` 重定向到 `/dashboard`。无路由守卫（原型阶段无鉴权需求）。

#### 3.4 TypeScript 类型体系

`types/index.ts` 共定义 **23 个接口**，覆盖 5 个业务域，全部使用字符串字面量联合类型代替 `enum`：

##### 设备域（6 个接口）

```typescript
interface Equipment {
  id: string
  name: string
  type: 'transformer' | 'breaker' | 'gis'
  model: string
  location: string
  status: 'online' | 'offline' | 'warning' | 'error'
  healthScore: number
  healthLevel: 'healthy' | 'attention' | 'abnormal' | 'critical'
  lastMaintenance: string
  installDate: string
  ratedVoltage: string
  ratedCapacity: string
}

interface EquipmentStats {
  total: number; online: number; warning: number; offline: number
}

interface EquipmentTypeDistribution {
  type: string; count: number; percentage: number
}

interface Alarm {
  id: string; equipmentId: string; equipmentName: string
  level: 'info' | 'warning' | 'danger' | 'critical'
  message: string; timestamp: string
  status: 'active' | 'acknowledged' | 'resolved'
}

interface AlarmTrend { date: string; count: number }

interface HealthDistribution {
  healthy: number; attention: number; abnormal: number; critical: number
}
```

##### 问答域（3 个接口）

```typescript
interface ChatMessage {
  id: string
  role: 'user' | 'assistant'
  content: string; timestamp: string
  confidence?: number; sources?: KnowledgeSource[]
}

interface KnowledgeSource {
  id: string; title: string
  type: 'standard' | 'manual' | 'history' | 'expert'
  relevance: number; excerpt: string
}

interface ChatSession {
  id: string; title: string
  createdAt: string; updatedAt: string
  messages: ChatMessage[]
}
```

##### 诊断域（10 个接口）

```typescript
interface SensorData {
  name: string; value: number; unit: string
  status: 'normal' | 'warning' | 'danger'
  threshold?: { min: number; max: number }
}

interface DGAData {
  h2: number; ch4: number; c2h2: number; c2h4: number; c2h6: number
  co: number; co2: number; totalHydrocarbon: number
}

interface AgentNode {
  id: string; name: string; nameEn: string
  status: 'pending' | 'running' | 'completed' | 'error'
  progress: number; description: string; result?: any
}

interface DiagnosisResult {
  equipmentId: string; equipmentName: string; timestamp: string
  overallConfidence: number
  rootCause: RootCauseAnalysis
  batchRisk: BatchRiskAnalysis
  fmea: FMEAAnalysis
  recommendation: MaintenanceRecommendation
}

interface RootCauseAnalysis {
  primaryCause: string; confidence: number
  evidenceChain: EvidenceNode[]; contributingFactors: string[]
}

interface EvidenceNode {
  id: string; label: string
  type: 'observation' | 'test' | 'analysis' | 'conclusion'
  children?: EvidenceNode[]; confidence?: number
}

interface BatchRiskAnalysis {
  affectedEquipment: string[]; riskLevel: string
  commonDefect: string; recommendation: string
}

interface FMEAAnalysis {
  failureMode: string; effect: string
  severity: number; occurrence: number; detection: number; rpn: number
}

interface MaintenanceRecommendation {
  actions: MaintenanceAction[]; priority: string
  estimatedCost: string; estimatedDuration: string
}

interface MaintenanceAction {
  id: string; title: string; description: string
  type: 'inspection' | 'repair' | 'replacement' | 'monitoring'
  urgency: string
}
```

##### 维护域（2 个接口）

```typescript
interface MaintenancePlan {
  id: string; equipmentId: string; equipmentName: string
  type: 'preventive' | 'corrective' | 'predictive'
  scheduledDate: string
  status: 'pending_review' | 'approved' | 'in_progress' | 'completed'
  priority: 'low' | 'medium' | 'high' | 'urgent'
  description: string; estimatedCost: string; assignedTo: string
}

interface WorkOrder {
  id: string; title: string
  equipmentId: string; equipmentName: string
  status: 'pending_review' | 'approved' | 'in_progress' | 'completed'
  priority: 'low' | 'medium' | 'high' | 'urgent'
  createdAt: string; scheduledDate: string
  description: string; actions: string[]
  estimatedCost: string; assignedTo: string
}
```

##### 通用 API 类型

```typescript
interface ApiResponse<T> { code: number; message: string; data: T }
interface PaginatedResponse<T> { items: T[]; total: number; page: number; pageSize: number }
```

#### 3.5 状态管理（Pinia Stores）

##### 3.5.1 useAppStore — 全局 UI 状态

| 成员 | 类型 | 说明 |
|---|---|---|
| `sidebarCollapsed` | `ref<boolean>` | 侧边栏是否折叠 |
| `currentPageTitle` | `ref<string>` | 当前页面标题（面包屑用） |
| `toggleSidebar()` | function | 切换侧边栏折叠状态 |
| `setPageTitle(title)` | function | 设置当前页面标题 |

##### 3.5.2 useEquipmentStore — 设备数据中心

| 状态 | 类型 | 说明 |
|---|---|---|
| `equipmentList` | `ref<Equipment[]>` | 设备列表 |
| `stats` | `ref<EquipmentStats>` | 设备统计 |
| `alarms` | `ref<Alarm[]>` | 告警列表 |
| `workOrders` | `ref<WorkOrder[]>` | 工单列表 |
| `healthDistribution` | `ref<HealthDistribution>` | 健康度分布 |

计算属性：`healthyEquipment`、`attentionEquipment`、`abnormalEquipment`、`criticalEquipment`（按 `healthLevel` 筛选子集）。

核心 Action `initMockData()` 初始化：10 台设备（5 个变电站、3 种类型）、7 条告警、4 条工单，并自动计算统计和健康度分布。

##### 3.5.3 useChatStore — 智能问答

| 状态 | 类型 | 说明 |
|---|---|---|
| `sessions` | `ref<ChatSession[]>` | 会话列表 |
| `currentSessionId` | `ref<string \| null>` | 当前会话 ID |
| `isLoading` | `ref<boolean>` | AI 回答中 |

核心 Action `sendMessage(content)` 模拟 AI 问答：添加用户消息 → 随机延迟 1.5-3 秒 → 返回预设领域回答（含 Markdown 格式、置信度、知识引用）。

##### 3.5.4 useDiagnosisStore — 故障诊断

| 状态 | 类型 | 说明 |
|---|---|---|
| `selectedEquipmentId` | `ref<string \| null>` | 选中设备 |
| `isDiagnosing` | `ref<boolean>` | 诊断进行中 |
| `diagnosisProgress` | `ref<number>` | 进度 0-100 |
| `agents` | `ref<AgentNode[]>` | 5 个 Agent 节点 |
| `diagnosisResult` | `ref<DiagnosisResult \| null>` | 诊断结果 |
| `sensorData` | `ref<SensorData[]>` | 传感器数据 |

核心 Action `startDiagnosis(equipmentId)` 模拟完整诊断流程：

1. 初始化 5 个 Agent（RCA / 批次风险 / FMEA / 处置决策 / 综合评审）
2. 加载 8 项传感器数据（油温、绕组温度、H2、C2H2、CH4、局放、铁芯接地电流、微水）
3. 依次驱动每个 Agent 经历 pending → running → completed 状态变化（含进度条动画）
4. 全部完成后设置诊断结果：根因分析（含递归证据树）、批次风险、FMEA（RPN=120）、4 条维护建议

#### 3.6 API 服务层（`services/api.ts`）

Axios 客户端配置：

- 基础路径：`/api/v1`
- 超时：30 秒
- 请求拦截器：从 `localStorage` 读取 `token` 并设置 `Authorization: Bearer` 头（预留鉴权）
- 响应拦截器：`code` 为 `0` 或 `200` 视为成功，其余弹出 `ElMessage.error`；HTTP 错误按状态码显示中文提示

API 模块分组（共 5 组 23 个方法）：

| 模块 | 方法数 | 端点前缀 | 说明 |
|---|---|---|---|
| `dashboardApi` | 5 | `/dashboard/` | 大屏统计、类型分布、告警趋势、健康分布、最新告警 |
| `equipmentApi` | 4 | `/equipment/` | 设备列表、详情、传感器数据、健康评分 |
| `chatApi` | 6 | `/chat/` | 会话 CRUD、消息发送、知识检索 |
| `diagnosisApi` | 4 | `/diagnosis/` | 启动诊断、状态查询、结果获取、历史记录 |
| `maintenanceApi` | 4 | `/maintenance/` | 计划查询、生成计划、工单管理 |

**重要说明**：API 层已完整定义并类型化，但当前所有页面均使用 Pinia Store 中的 Mock 数据，API 层处于"就绪待接入"状态。

#### 3.7 页面组件设计

##### 3.7.1 DashboardView — 可视化大屏（约 700 行）

三列网格布局的指挥中心大屏：

| 区域 | 列 | 内容 |
|---|---|---|
| 顶部 | 全宽 | 系统中英文标题、实时时钟（每秒更新） |
| 左列 | `grid-col--left` | 4 个 StatCard 统计卡片、设备类型饼图、7 日告警折线图 |
| 中列 | `grid-col--center` | 健康度分布双环饼图（外环分类 + 内环总数）、4 个 CSS 圆环指标 |
| 右列 | `grid-col--right` | 最新告警列表（可滚动）、知识检索输入框 + 快捷标签 |

ECharts 图表（3 个）：

1. **设备类型分布**：环形饼图，紫/橙/青三色
2. **告警趋势**：平滑折线 + 面积渐变填充
3. **健康度分布**：嵌套双饼图，外环四类（健康/关注/异常/危急），内环透明显示总数

数据来源：`useEquipmentStore`

##### 3.7.2 ChatView — 智能问答（约 800 行）

ChatGPT 风格双栏对话界面：

| 区域 | 内容 |
|---|---|
| 左侧边栏（260px） | 会话历史列表，支持新建/删除/切换 |
| 主区域 | 消息区（可滚动）+ 底部输入区 |

核心特性：

- **自定义 Markdown 渲染器**：基于正则表达式的轻量实现（标题、粗体、斜体、引用、列表），未使用 `marked` 库
- **置信度可视化**：每个 AI 回答附带圆形进度条（>=80% 青色、>=60% 黄色、<60% 红色）
- **知识来源展示**：可折叠面板显示引用来源，含类型标签（标准/手册/历史/专家）和相关度百分比
- **快捷提问**：预设常见问题按钮（空状态和输入框下方）
- **打字指示器**：3 个弹跳圆点动画
- **自动滚动**：`watch` 监听消息列表变化自动滚动到底部

数据来源：`useChatStore`

##### 3.7.3 DiagnosisView — 故障诊断（约 1050 行）

工具栏 + 三列诊断工作台：

| 区域 | 内容 |
|---|---|
| 工具栏 | 设备选择下拉框、「开始诊断」按钮、进度条 |
| 左列（280px） | 8 项传感器数据卡片（含状态指示和阈值标注） |
| 中列 | 5 Agent 流水线可视化（顺序节点 + 连接线） |
| 右列 | Tab 页诊断报告（4 个 Tab） |

**内联组件 EvidenceTree**：使用 `defineComponent` + `h()` 渲染函数实现的递归证据树组件。每个节点显示类型图标（圆/三角/方/星对应观察/测试/分析/结论）、标签和置信度，支持多层嵌套。

**5 个 Agent 节点**：

| # | Agent | 功能 | 英文名 |
|---|---|---|---|
| 1 | RCA Agent | 根因分析 | Root Cause Analysis |
| 2 | Batch Risk Agent | 批次风险评估 | Batch Risk Assessment |
| 3 | FMEA Agent | 失效模式分析 | Failure Mode & Effects |
| 4 | Decision Agent | 处置决策 | Maintenance Decision |
| 5 | Review Agent | 综合评审 | Comprehensive Review |

Agent 流水线可视化：纵向排列，节点间有连接线。每个节点经历 pending（灰色）→ running（旋转图标 + 环形进度）→ completed（勾选图标 + 紫色高亮）。

**报告 Tab 页（4 个）**：

1. **基础信息**：设备名称、时间戳、总体置信度、优先级、预估费用/工期
2. **根因分析**：主要原因 + 置信度徽章、递归证据树、 contributing factors
3. **批次风险**：风险等级徽章、共性缺陷描述、受影响设备标签、建议
4. **处置建议**：操作卡片（按类型着色：监测/巡检/维修/更换）+ FMEA 网格（S/O/D/RPN 评分）

数据来源：`useEquipmentStore` + `useDiagnosisStore`

##### 3.7.4 MaintenanceView — 检修决策（约 794 行）

两行布局的维护管理面板：

| 区域 | 内容 |
|---|---|
| 上行 | 健康度仪表盘（ECharts Gauge）+ 健康分布条（Element Plus Progress） |
| 下行左列 | 设备健康表格（可按等级筛选） |
| 下行右列 | 工单列表（展开/折叠卡片） |

ECharts 图表（1 个）：

- **健康度仪表盘**：220° 到 -40° 弧度，渐变色带（红→橙→黄→青），无指针/刻度的极简设计

设备表格列：名称（含类型图标）、健康评分（颜色编码）、状态徽章、位置、推荐操作。

工单卡片：可展开显示设备信息、负责人、计划日期、费用、描述和操作清单。

「生成计划」按钮为选中设备自动创建工单，根据健康指数自动计算优先级。

数据来源：`useEquipmentStore`

#### 3.8 通用组件设计

##### 3.8.1 布局组件

**AppLayout**：主布局外壳，`Sidebar`（固定左侧）+ `main-area`（`HeaderBar` + `<router-view>` 含 fade 过渡）。在 `onMounted` 中调用 `equipmentStore.initMockData()` 和 `chatStore.initMockData()` 完成全量 Mock 数据初始化。

**Sidebar**：固定定位侧边栏，240px ↔ 64px 折叠切换（0.3s ease 过渡）。4 个导航项对应 4 个路由，诊断菜单显示告警数徽章。

**HeaderBar**：面包屑导航 + 搜索框（预留）+ 通知铃铛（未读数 badge）+ 用户头像。

##### 3.8.2 业务组件

**StatCard**：动画统计卡片，数字从 0 到目标值 800ms 渐变动画。Props: `icon`、`value`、`label`、`color`（primary/success/warning/danger）、`suffix`。

**StatusBadge**：状态徽章，支持 5 种颜色主题和可选脉冲动画圆点。Props: `type`、`text`、`dot`。

#### 3.9 样式主题设计

采用「清华紫」暗色主题，SCSS 变量定义在 `variables.scss` 中：

| 分类 | 变量 | 值 | 用途 |
|---|---|---|---|
| 主色 | `$primary-color` | `#791CB5` | 品牌紫 |
| | `$primary-dark` | `#5B1499` | 深色变体 |
| | `$primary-light` | `#9B4DCA` | 浅色变体 |
| 语义色 | `$accent-color` | `#FF8C38` | 橙色强调 |
| | `$success-color` | `#26C6DA` | 青色成功 |
| | `$warning-color` | `#FFB300` | 琥珀警告 |
| | `$danger-color` | `#E53935` | 红色危险 |
| 背景 | `$dark-bg` | 135deg 渐变 | 页面背景 |
| | `$dark-bg-card` | `rgba(123,28,181,0.15)` | 卡片背景 |
| 文字 | `$text-primary` | `#ffffff` | 主文字 |
| | `$text-secondary` | `rgba(255,255,255,0.75)` | 次文字 |
| 布局 | `$sidebar-width` | `240px` | 展开侧边栏 |
| | `$header-height` | `60px` | 顶部导航 |

Element Plus 主题通过 `:root` CSS 变量覆盖 `--el-color-primary` 系列色值。全局样式包含自定义滚动条（紫色调）、4 组关键帧动画（fadeIn / pulse / slideInLeft / glow）和 Vue 过渡类。

#### 3.10 模块依赖关系

```
main.ts
  └── App.vue
        └── router/index.ts
              └── AppLayout.vue (eager)
                    ├── Sidebar.vue
                    │     ├── useAppStore
                    │     └── useEquipmentStore
                    ├── HeaderBar.vue
                    │     ├── useAppStore
                    │     └── useEquipmentStore
                    └── <router-view> (lazy)
                          ├── DashboardView.vue
                          │     ├── useEquipmentStore
                          │     ├── StatCard.vue
                          │     └── StatusBadge.vue
                          ├── ChatView.vue
                          │     └── useChatStore
                          ├── DiagnosisView.vue
                          │     ├── useEquipmentStore
                          │     ├── useDiagnosisStore
                          │     └── StatusBadge.vue
                          └── MaintenanceView.vue
                                ├── useEquipmentStore
                                └── StatusBadge.vue

数据流:
  services/api.ts → types/index.ts (已定义, 待接入)
  stores/*.ts → types/index.ts (当前数据源)
```

---

### 四、前后端接口对应关系

下表展示前端 API 层定义与后端实际端点的映射关系。当前前端使用 Store Mock 数据，后续可通过切换数据源无缝对接后端。

| 前端 API 方法 | 后端端点 | 对齐状态 |
|---|---|---|
| `chatApi.sendMessage()` | `POST /api/v1/chat` | 路径需微调（`/sessions/:id/messages` vs `/chat`） |
| `diagnosisApi.startDiagnosis()` | `POST /api/v1/diagnosis/trigger` | 路径需微调（`/start` vs `/trigger`） |
| `diagnosisApi.getDiagnosisResult()` | `GET /api/v1/diagnosis/{id}` | 已对齐 |
| `maintenanceApi.generatePlan()` | `POST /api/v1/maintenance/plan` | 已对齐 |
| `equipmentApi.getList()` | `GET /api/v1/equipment` | 已对齐 |
| `equipmentApi.getById()` | `GET /api/v1/equipment/{id}` | 已对齐 |
| `equipmentApi.getHealthScore()` | `GET /api/v1/equipment/{id}/health` | 路径需微调 |
| `dashboardApi.getEquipmentStats()` | `GET /api/v1/equipment/stats` | 路径需微调（`/dashboard/` vs `/equipment/`） |
| `chatApi.searchKnowledge()` | `GET /api/v1/knowledge/search` | 路径需微调 |

---

### 五、关键设计模式与技术决策

**后端：**

1. **统一响应包装**：所有端点返回 `ApiResponse[T]`，异常也返回 HTTP 200（code 非 0），符合国内企业 API 设计惯例
2. **异步预留**：所有 Service 方法声明为 `async`，虽然当前 Mock 数据是同步的，但为接入真实数据库/LLM 预留了异步接口
3. **依赖注入**：通过 FastAPI `Depends()` + 工厂函数实现 Router 与 Service 的松耦合
4. **WebSocket 实时通信**：故障诊断模块使用 WebSocket 推送多 Agent 协作进度，模拟 SSE 事件流

**前端：**

1. **Composition API 全量使用**：所有组件和 Store 均使用 `<script setup>` + Composition API
2. **Mock 优先**：Store 层 `initMockData()` 提供完整业务数据，API 层就绪待切换
3. **自动导入**：`unplugin-auto-import` + `unplugin-vue-components` 消除 Vue/Pinia/Element Plus 的显式 import
4. **SCSS 全局注入**：`variables.scss` 通过 Vite `additionalData` 注入每个样式块，无需重复 import
5. **渲染函数组件**：`EvidenceTree` 使用 `defineComponent` + `h()` 实现递归渲染，展示 Vue 高级用法
6. **CSS 动画代替图表**：Dashboard 的 4 个指标环使用纯 CSS `conic-gradient` 实现，性能优于 ECharts
